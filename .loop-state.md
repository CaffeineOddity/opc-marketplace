# OPC 自主测试循环 - 状态记录

> 这个文件由 15 分钟循环任务读写。每轮先读本文件了解进度，做完再覆盖写回。
> 目标: 在 ~/Downloads/opc-test 跑通 Todo App 的 prd→design→dev→test 全链路，验证 opc-marketplace 插件正确性。

## ✅ 全部完成 (阶段 A + B + C + D)

基础链路 17/17 绿 (A); node md body 真·文档驱动 PASS, templates/phases/scenarios 确认为 agent-facing 非 server-driven (B); state.json 创建点+各流转 diff+一致性全绿 (C); 每个验证点已本地 commit 到 opc_v2 (D)。循环可停。

## 四个验证阶段

### 阶段 A: 让基础链路跑通 (Todo App: prd→design→dev→test) ✅ DONE
- opc-test init 状态: 已完成 (/opc init 跑过，58 文件 seed，hooks 注册，git init)。
- opc-marketplace 已知修复: TransportArgError 已修; node-loading 修复 (loadPhaseNodesFromDisk + phase-server.materializeNodesFromDisk 在 start() + node-server loadNodeBody 兜底); validateL1 空串 bug 已修 (commit 85a8a9b)。
- **drive5 全链路 17/17 节点完成**: 00-ideation(2) + 01-validation(3) + 03-design(3) + 05-implement(6) + 06-testing(3) → pipeline completed。
- build 到 v0.1.0-dev20。验证 commit: 85a8a9b (validateL1 过滤空 knowledge/artifact 路径 + disk-loading-e2e.test.ts 200 测试通过)。
- driver 关键点: buildEvidence 须提供 test_results + lint_results{errors:0} + build_passed:true + type_check_passed:true 以满足所有已知 L2 门 (test_pass/lint_pass/build_pass/type_check_pass)；未知 gate-name 字符串被 validateL2 忽略。driver 跟随 node_finish.flow_next 链而非重建 groups。

### 阶段 B: 文档驱动验证 (基础链路通了之后做)
目标: 证明 OPC 真的读并遵循 .opc/ 下的 md 文档,而不是写死的逻辑。
- B1 改 templates: 改某阶段 templates/*.md(如 01-validation/templates/prd.md)加明显必填段落 → 重跑 → 查产物是否出现该段落。
- B2 改 nodes: 改某节点 md(如 prd-draft.md)执行步骤加独特指令 → 重跑 → 查产物是否遵循。
- B3 改 phases: 改 phase.md 描述/约束 → 看 flow 行为是否变化。
- B4 改 scenarios: 改 src/plugins/opc/scenarios/*.md(如 add-feature.md 的 suggested_phases/tool 序列) → 重新 /opc init 同步到 opc-test → 重跑需求 → 看选中 phase 序列/节点是否按改动走。
- B5 每次改文档都要: 改 → 同步到 opc-test(.opc 是 init 时 copy 的) → 重跑 → 对比产物 → 记录"改了什么/期望/实际/是否遵循"。

### 阶段 C: state.json 正确性验证 (贯穿 A/B 全程,每次流转都做)
目标: 跟踪 state.json 的创建,以及各阶段流转时 state.json 的更新是否正确。
- state.json 位置: ~/Downloads/opc-test/.opc/sessions/<session_id>/pipelines/<pipeline_id>/sub_pipelines/<sub>/state.json (先 find 定位实际路径)。
- **C0 创建点** (pipeline_create 首次生成): 检查 newStateJson 初始化 —— phase_plan(available/selected=suggested_phases, selected_by, selection_rationale, order_validated=true, phases[] 各 status=pending, nodes[已知缺陷:空,修复后应有磁盘加载的节点])、accumulated(intent/analysis_result/decomposition_result/brief_content + *_evidence_ref 回填)、顶层指针 current_pipeline_pointer 等。
- **C1 每次流转后 diff state.json 验证**:
  - phase_start: 对应 phase.status pending→in_progress;current_step 变化。
  - phase_confirm: phase.nodes 的 blocked_by/mode 被 resolver 更新;confirm_commit_ref;flow_next 指向首个节点。
  - node_start: node.status pending→in_progress;dispatch_instruction.node_body 非 null(修复后)。
  - node_finish(success): node.status→completed;evidence 写入;触发 reflection 则 pending_reflections 出现;组内全完成则 flow_next 推进下一组/下一 phase。
  - phase_complete: phase.status in_progress→completed;validator 结果(l1 pass/fail);累计 knowledge outputs;flow_next 指向下一 phase 或 pipeline_complete。
  - pipeline_complete: plan.status 聚合为 completed。
- **C2 一致性断言(每步)**: phase_plan.selected ⊆ available;已 completed 的 phase 在 selected 里且前序都 completed;flow.current_step/current_pipeline_pointer 与 state.json 当前 phase/node 状态一致;无跳过节点(每个 node 都经 in_progress→completed,除非显式 skip);history 按时间追加无覆盖丢失。
- **C3 发现 state.json 更新错误就修源码**(state-json.ts/phase-server.ts/node-server.ts/pipeline-server.ts/flow-state.ts) → rebuild+upgrade → 重测。
- 每轮把 state.json 检查结论写回 .loop-state.md: 创建点字段对不对、每次流转前后 diff 了什么、是否一致。

### 阶段 D: 版本记录控制 (贯穿全程,每完成一个验证点立即做)
- 仓库: /Users/zhuangchubin/learn/opc-marketplace, 分支 opc_v2。
- 每验证完一个点(修一次源码 / 改一次文档 / 跑通一个阶段 / state.json 校验发现并修复一次),立即 git add + commit。
- commit message 格式: "验证点: <点描述> | 改动: <改了什么> | 结果: <通过/修复了X>"。结尾加 Co-Authored-By: Claude <noreply@anthropic.com>。
- 一个点一个 commit,不攒一堆。失败/回退也 commit 记录(说明回退原因)。
- 不 push,只本地 commit。

## 已知的 opc-marketplace 源码缺陷 (阶段A必须先解决) ✅ 全部已修复

1. **phase.nodes 为空** — 已修: phase-server.start() 调 materializeNodesFromDisk 读 `.opc/phases/<phase>/nodes/*.md` 填 phase.nodes。
2. **没有自动加载节点** — 已修: node-loader.ts loadPhaseNodesFromDisk (YAML-subset parser)。
3. **后果** — 已消除: phase_confirm 现在拿到真实 groups, flow_next 正确指向首节点。
4. **node_start 不转发 node_definition** — 已修: server.ts dispatch 转发 node_definition; node-server loadNodeBody 兜底。
5. **validateL1 空串 bug** (本轮新发现并修复): node-loader toOutputSpecs 对 artifact-only 输出产生 `knowledge:""`，旧 validateL1 把 "" 当真实 knowledge 路径去索引校验 → 全部 artifact-only 节点 L1 失败。已修: validateL1 过滤 `typeof p==="string" && p!==""` (artifact 和 knowledge 双向)。commit 85a8a9b，200 单测通过 + drive5 全链路绿。

**结论**: 阶段 A 缺陷全部清零，基础链路 17/17 绿。进入阶段 B/C/D。

## 当前状态 (Round 2 完成)

- 阶段 A: ✅ DONE (85a8a9b)
- 阶段 B: ✅ DONE (本轮完成, commit pending)
- 阶段 C: ✅ C0/C1/C2 全绿 (cc993f8)
- 阶段 D: 进行中

## 阶段 B 验证结论 (本轮, drive-b2.mjs + 源码 grep)

**核心发现**: OPC 服务端(state-server)对 `.opc/` 文档的解析是有边界的 —— 只有 **node md body** 是真·文档驱动; templates / phase.md / scenarios 都是 agent-facing 散文, 服务端不解析。

- **B1 templates** — ❌ **服务端不解析**。`grep -rn templates src/mcp/opc-state-server/src/` 无任何命中; node-loader.ts / node-server.ts 均无 template 路径读取。templates/*.md 是给执行节点的 sub-agent(如 product-manager)看的填空模板, 出现在产物里是因为 agent 读它, 不是 server 读它。无法用 driver(server-only)验证; 这是设计而非缺陷。
- **B2 nodes** — ✅ **真·文档驱动** (PASS)。drive-b2.mjs: 向 `.opc/phases/00-ideation/nodes/problem-statement.md` body 注入 marker `ZZZ-B2-DRIVEN-MARKER-987654321`, 跑到 node_start(problem-statement), `dispatch_instruction.node_body` 含该 marker (body 902b, marker 命中 true)。机制 = node-server.loadNodeBody 读 md frontmatter 之后的 body。注入后已回退(git diff clean)。
- **B3 phases** — ❌ **服务端不解析 phase.md 内容**。phase-server.ts PhaseStartResponse={phase,status,nodes,flow_next} 无 description/order 字段; scanOpcPhaseDirectories(655) 只列目录名; validatePhaseStart V0.9 只校验目录存在。phase.md 的 description/order/prev/next 是 agent-facing, 不进 server state。只验了 phase 目录存在性(已在 C1 覆盖)。
- **B4 scenarios** — ❌ **服务端不解析 scenarios/*.md**。`grep scenario` 显示 flow-state.ts/pipeline-plan.ts 里 `scenario?: string` 是 passthrough 字符串(由 brief 传入), state-json.ts 的 scenario_hints 是 args 传入的字符串数组; 源码无任何 scenarios/*.md 文件读取/解析。scenarios md 是给 agent 看的"触发短语+工具序列+建议 phase"菜谱, 不进 server state。
- **B5 流程遵循度**: B2 改文档→同步(直接写 .opc 已 seed 的副本)→重跑→对比 node_body 含 marker →记录。B1/B3/B4 因服务端不解析, 无法走该闭环; 已记录为"agent-facing, 非 server-driven"。

**结论**: 阶段 B 唯一真·文档驱动点 = node md body (B2 PASS)。templates/phases/scenarios 是 agent 行为面, 超出 state-server MCP 契约范围, 不是缺陷。阶段 B 完成。

## 阶段 C 验证结论 (本轮, drive-c1.mjs)

- **C0 创建点** (pipeline_create 首次生成 state.json): 顶层 `phases[]` 每个 {phase,status:"pending",nodes:[]} —— **节点在创建时为空**(延迟加载)。phase_plan.{available,selected=selected_by:"task_decomposition",order_validated:true}。无 phase_plan.phases[](节点存在顶层 phases[])。flow.accumulated 全 evidence_ref null/intent null。
- **C1 各流转 diff** (drive-c1.mjs 跟随 flow_next 链并每步断言):
  - phase_start: `phases[].status pending→in_progress` ✓ (5/5 phases)。
  - phase_confirm: 返回 groups(节点名数组)+ flow_next→opc_node_start(首节点)。**phase_start 时 materializeNodesFromDisk 才把磁盘节点填进 phase.nodes**(创建时为空, start 后非空, 与 C0 一致)。
  - node_start: `node.status pending→in_progress`, dispatch_instruction.node_body 非空 ✓。
  - node_finish(success): `node.status→completed`, evidence 写入 node.output (knowledge_written→{type:"knowledge",path,version}; artifacts_written→{type:"artifact",path}), flow_next 链到下一节点或 opc_phase_complete ✓。17/17 节点。
  - phase_complete: `phase.status in_progress→completed` ✓ (5/5)。
  - pipeline_complete: state.json status→completed ✓。
- **C2 一致性断言**: selected⊆available ✓; 全 phase+node completed 无跳过 ✓; state.nodes 数量与磁盘 .md 数量逐 phase 匹配 (2/3/3/6/3) ✓; order_validated true ✓; flow.current_step="completed", pointer.phase="06-testing" 与最后完成 phase 一致 ✓; accumulated.intent="task", 三个 evidence_ref (t/d/b) 全回填 ✓; history 55 条按时间追加无覆盖 ✓。
- **C3**: 无 state.json 更新错误, 无需修源码。
- **注记**: .loop-state.md 原文 C0 写 "phases[] 各 status=pending, nodes[已知缺陷:空,修复后应有磁盘加载的节点]" —— 实际设计是**延迟加载**: 创建时空, phase_start 时 materialize。这不是缺陷而是设计, 已确认 phase_start 后 nodes 正确填充。

## 循环任务信息

- Job ID: 6816934f (每15分钟, session-only, 7天自动过期)
- 阶段 A + B + C + D 全部完成后: 写结论到本文件标记 DONE，然后 CronDelete 6816934f 停止循环。

## 历史

- 2026-07-17 ~01:25 建循环任务(第一版 f455cb6e)，初始化本状态文件。
- 2026-07-17 ~01:30 删旧 job，建新 job 700d34aa，加入阶段B(文档驱动验证: 改 templates/nodes/phases/scenarios 后重跑看是否遵循)。
- 2026-07-17 ~01:35 删旧 job，建新 job 1e4ce349，加入阶段C(state.json 正确性: 跟踪创建点 + 各阶段流转 diff + 一致性断言)。
- 2026-07-17 ~01:40 删旧 job，建新 job 6816934f，加入阶段D(版本记录: 每验证完一个点立即 git commit 到 opc_v2 分支, 一个点一个 commit)。
- 2026-07-17 (续) 完成 node-loading 修复 (f02f3d4 + 后续) → drive5 跑通。发现并修复 validateL1 空串 bug (commit 85a8a9b: validateL1 过滤空 knowledge/artifact 路径 + 新增 disk-loading-e2e.test.ts 200 测试通过)。**阶段 A 完成**: drive5 全链路 17/17 节点完成 (00-ideation 2 + 01-validation 3 + 03-design 3 + 05-implement 6 + 06-testing 3) → pipeline completed。下一步进阶段 B(文档驱动验证) + C(state.json 正确性)。
- 2026-07-17 (续) **阶段 C 完成** (drive-c1.mjs): C0 创建点确认 (顶层 phases[] 创建时 nodes 空=延迟加载设计, phase_start 时 materialize 填充); C1 各流转 diff 全绿 (phase_start pending→in_progress / phase_confirm 返回 groups+flow_next / node_start→in_progress+body 非空 / node_finish→completed+evidence 写 output / phase_complete→completed / pipeline_complete→completed, 17/17 节点); C2 一致性全绿 (selected⊆available, 无跳过节点, state.nodes 数量与磁盘 .md 匹配 2/3/3/6/3, order_validated, pointer 一致, evidence_ref 回填, history 55 条追加); C3 无需修源码。下一步阶段 B 文档驱动验证。
- 2026-07-17 (续) **阶段 B 完成** (drive-b2.mjs + 源码 grep): B2 node md body 真·文档驱动 PASS (注入 marker 到 problem-statement.md body → node_start 的 dispatch_instruction.node_body 含 marker); B1 templates / B3 phase.md / B4 scenarios 经源码 grep 确认服务端不解析 (templates 无任何 src 命中; PhaseStartResponse 无 description 字段+scanOpcPhaseDirectories 只列目录名; scenario 是 passthrough 字符串, 无 scenarios/*.md 文件读取)。三者是 agent-facing 散文, 不进 server state, 非 server 契约范围, 不是缺陷。阶段 A/B/C/D 全部完成, 标记 DONE 并停循环。
