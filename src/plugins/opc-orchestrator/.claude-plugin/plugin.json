{
  "name": "opc-orchestrator",
  "version": "0.1.0",
  "description": "OPC orchestrator: UserPromptSubmit hook that nudges Claude to call opc_flow_query, plus the /opc-status slash command and opc-status CLI (read-only terminal health snapshot, M18.h).",
  "depends": ["mcp"],
  "mcp": "./.mcp.json",
  "hooks": {
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/bin/opc-hook.sh"
          }
        ]
      }
    ]
  },
  "scenarios_dir": null,
  "_scenarios_note": "scenarios now sourced from .opc/scenarios/, bootstrapped by opc-state-server on first run"
}
