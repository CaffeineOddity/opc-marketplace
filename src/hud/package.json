{
  "name": "opc-hud",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && npm run bundle",
    "bundle": "esbuild ../../build-in/hud/index.js --bundle --platform=node --format=cjs --outfile=../../build-in/hud/opc-hud.bundle.cjs --external:fs --external:path --external:crypto --external:child_process --external:os --external:util"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.0.0"
  }
}
